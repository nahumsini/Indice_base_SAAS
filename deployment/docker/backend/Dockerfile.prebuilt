ARG BASE_IMAGE
FROM ${BASE_IMAGE}

# Offline deployment path for restricted VPS hosts. The JVM and non-root
# runtime come from the previously approved image; only the verified JAR is
# replaced.
COPY --chown=spring:spring target/*.jar /app/app.jar
