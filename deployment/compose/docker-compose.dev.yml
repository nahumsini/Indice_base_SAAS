services:
  mysql:
    ports:
      - ${MYSQL_HOST_PORT:-3307}:3306

  minio:
    ports:
      - ${MINIO_CONSOLE_HOST_PORT:-9001}:9001

  backend:
    ports:
      - ${BACKEND_HOST_PORT:-8082}:8082

  face-service:
    ports:
      - ${FACE_SERVICE_HOST_PORT:-8091}:8091
