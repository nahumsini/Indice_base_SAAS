# Production commercial presentation — 2026-09-22

Production at `https://app.indiceapp.com/presentation` received a frontend-only update on
2026-09-22 at 16:30 UTC. The user explicitly authorized publishing the commercial presentation.

The deployed source is commit `c8bd819c` on `release/presentation-20260922`, based on production
main `df44e086`. It adds the six-section commercial story, code-native diagrams, local presentation
images, fullscreen mode, and the Mexico price card `commercial-master-2026-09-21`:

- Controla MXN 2,999, Escala MXN 5,499, and Corporativo MXN 9,499 monthly.
- Ten people included; an additional block of ten is MXN 899 monthly.
- Annual billing applies the published 20% discount.
- Promotional setup through October 2026 is MXN 4,999, MXN 7,499.50, and MXN 12,499.50.

## Validation

- 24 focused presentation regressions passed on the isolated release worktree.
- TypeScript and the Vite production build passed. The existing shared-chunk size warning remains.
- The local production artifact passed the Chrome harness across all presentation sections, eight
  locales, 320/390/768/1440 px layouts, keyboard/history/reload behavior and fullscreen entry/exit.
- The same Chrome harness passed against the public production URL with zero runtime errors and zero
  business API requests.
- The canonical production smoke test passed for HTTPS, backend health, object storage, CSRF and the
  reserved invalid synthetic login, which returned the expected 401.
- The public HTML and container HTML both have SHA-256
  `4dac3d659b3b8ecb5e5dd47433ab6066b1964fe28893cab9a5644aff3c9bac9d` and reference
  `/assets/index-Dp3O8_2s.js`. The transferred archive has SHA-256
  `54adb51dc88dc91aeca799d054c25cc6490447020b2f7d34970612110775b03a`.
- Web, backend, MCP and MinIO remained running with zero restarts. No backend, database, migrations,
  billing catalog, Stripe configuration, MCP configuration or tenant data changed.

## Rollback and persistence

The previous frontend is retained on the VPS under
`/home/corazon/indice-presentation-c8bd819c/rollback/`. Its `index.html` SHA-256 is
`cb1cf9995b5195af9a85a3184c067af07283227550c88eac452e113cdc49cf38` and references
`/assets/index-AbYAY0nR.js`. Restoring that directory to
`indice-erp-web-1:/usr/share/nginx/html/` reverts this frontend-only publication.

The running image remains `indice-erp-web:74fcf8b7d5e2`; this update is in the container writable
layer, matching the repository's documented frontend-only publish flow. A future container
recreation must build from or reapply `release/presentation-20260922` so the presentation is not
lost. The backend, MCP and MinIO images remain unchanged.
