# v2026.09.19.1 — Integración de main, KPIs y presentación pública

Producción en `https://app.indiceapp.com` se actualizó el 19 de septiembre de 2026
a las **09:51:24 UTC**, con autorización explícita del usuario para ese ambiente.
El código desplegado es `74fcf8b7d5e2907815cfc1468f07a8961ca39658`, publicado en
`main` y aprobado por [CI del commit exacto](https://github.com/nahumsini/Indice_base_SAAS/actions/runs/35434766461).

La versión reúne la [integración local del 19 de septiembre](../../docs/indice-local-integration-2026-09-19.md)
y la [integración de KPIs y presentación pública](../../docs/indice-kpi-main-integration-2026-09-19.md).
Incluye las mediciones de los módulos básicos, las fuentes autoritativas del panel
ejecutivo y las rutas públicas `/investment` y `/Mrcarlosmunoz`. El registro presente
documenta la operación; no introduce cambios adicionales en el código desplegado.

## Artefactos y verificaciones

Las tres imágenes se construyeron en el VPS para `amd64`, desde el checkout exacto.

| Imagen | Identificador comprobado |
| --- | --- |
| `indice-erp-backend:74fcf8b7d5e2` | `sha256:b7534e2c64794144da832ab6d5e724cc7b97d5f6159d47a8e52570bcd305940d` |
| `indice-erp-web:74fcf8b7d5e2` | `sha256:06a4d7d83983273b7e80001b61c9c3649aaf8d8f686e13728b3b19dd02ed130e` |
| `indice-erp-mcp:74fcf8b7d5e2` | `sha256:80f2c16432efdfca8c2520fc51b1314c24c6a706d1b9dd24b2d561d8e5fa7314` |

- Frontend local: 802 pruebas aprobadas, comprobaciones complementarias de
  localización y teléfono, TypeScript y build de producción aprobados.
- Backend: 2.466 pruebas en 453 clases, sin fallos, errores ni omisiones, tanto
  localmente como en el preflight del VPS. Las pruebas usaron MySQL aislado.
- MCP: 27 pruebas aprobadas. Preflight con el archivo productivo protegido y la
  topología host-network aprobado; las imágenes se construyeron por separado.
- Trivy 0.69.3: cero hallazgos HIGH/CRITICAL en las tres imágenes y en las
  dependencias desplegables. SBOM CycloneDX conservados. El escaneo del código
  no encontró secretos; Gitleaks 8.24.2 tampoco encontró secretos en el historial
  incorporado desde la versión anterior.
- El escaneo general encontró ocho hallazgos HIGH en los lockfiles del demo
  histórico `demoIndice/`. Ese directorio no cambió respecto al artefacto anterior
  y no se copia ni construye en ninguna de las tres imágenes; queda fuera de esta
  liberación. Los informes lo distinguen de los artefactos desplegables.
- Ensayo final aprobado con el umbral normal de 10 GiB de espacio libre.
- Smokes de web, backend, MinIO, CSRF y login sintético inválido aprobados. Las
  cookies de sesión comprobadas por HTTPS conservan `Secure` y `HttpOnly`.
- Los endpoints de KPIs de Ventas, opciones organizativas y MCP rechazan acceso
  anónimo con HTTP 401. La verificación pública de MCP aprobó sus metadatos OAuth,
  OIDC y soporte.
- El HTML público coincide byte por byte con la imagen desplegada y sus ocho
  assets de entrada responden HTTP 200. SHA-256 del HTML:
  `cb1cf9995b5195af9a85a3184c067af07283227550c88eac452e113cdc49cf38`.
- Chrome sobre producción: ocho idiomas, siete pestañas y variante personalizada,
  anchos de 320/390/768/1440, menús, historial, recarga, teclado y tema oscuro.
  Cero errores de ejecución, recursos fallidos o solicitudes a APIs de empresa.

La preparación resolvió dos incidencias de infraestructura: DNS del bridge Docker
mediante la red del host, y el nombre de la base temporal, que dos pruebas exigen
que sea exactamente `indice_test_db`. El preflight completo se repitió y aprobó
sin cambiar código ni aserciones. Persiste el aviso conocido de chunks grandes
del frontend; el build aprobó.

## Conservación de datos y operación

La configuración de aplicación, autenticación, datasource, Flyway y MCP coincide
con la de los contenedores anteriores. Se conservaron la imagen y el volumen de
MinIO. No se activaron nuevas banderas comerciales.

Flyway permanece en **V276**, sin migraciones nuevas ni fallidas. Las auditorías
de alcance y propiedades visuales devolvieron cero incidencias. Coincidieron las
huellas del catálogo de facturación, suscripciones por versión, catálogos públicos
y sus productos; se almacenaron huellas, no filas de clientes, en esa evidencia.

Web, backend, MCP y MinIO quedaron activos con cero reinicios y cero líneas
ERROR/FATAL en la comprobación posterior. Los contenedores de APPTEST conservaron
sus identificadores, imágenes y horas de arranque. Quedaron aproximadamente 24 GiB
libres en el VPS.

## Respaldo y rollback

Los respaldos protegidos y sus sumas SHA-256 permanecen en:
`/home/corazon/apps/indice-erp-docker/backups/pre-74fcf8b7d5e2-20260919T094550Z/`.

Se comprobó la restauración completa de `production.sql.gz` en MySQL aislado,
sin red: 314 tablas/vistas, V276 y cero migraciones fallidas. Se extrajo completo
`minio.tar.gz`, se comparó la suma de una muestra y se arrancó MinIO con los datos
restaurados en puertos loopback temporales; readiness respondió HTTP 200. Los
contenedores, volúmenes, credenciales temporales y copia restaurada se retiraron
al terminar.

La versión anterior `e26442a51fac` permanece detenida bajo
`indice-erp-*-1-rollback`. Los contenedores de rollback que existían antes se
conservaron con el sufijo `-rollback-preserved-74fcf8b7d5e2`. No se eliminaron
imágenes históricas ni volúmenes productivos. La reversión de aplicación es
compatible con el esquema V276:

```bash
CONFIRM_ROLLBACK=true HOST_WEB_PORT=8080 HOST_BACKEND_PORT=8083 \
HOST_MINIO_API_PORT=9000 MCP_ENABLED=true HOST_MCP_PORT=3011 \
bash /root/indice-candidate-74fcf8b7d5e2/source/deployment/scripts/rollback-host-network.sh
```

## Alcance de la evidencia

El checkout dedicado y la evidencia operativa permanecen en
`/root/indice-candidate-74fcf8b7d5e2/`: preflight, escaneos, SBOM, restauración,
activación, auditorías, comprobaciones HTTP y revisión pública en navegador.
Los respaldos y las credenciales no se incorporan al repositorio.

Migraciones nuevas, activación de Stripe y cambios de DNS: N/A. No se hicieron
transacciones de prueba con cuentas reales ni UAT de MFA o dispositivos físicos.
La publicación de la SPA no configura las rutas del dominio raíz `indiceapp.com`;
las presentaciones verificadas están en `app.indiceapp.com`. Esta evidencia no
constituye una certificación integral de seguridad.
