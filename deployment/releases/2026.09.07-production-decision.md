# Production activation: v2026.09.06.1

Production at `https://app.indiceapp.com` was activated on 2026-09-07 at 04:04:42 UTC.
The deployed source is release tag `v2026.09.06.1`, commit
`9e56ed294111843dda5bc0da53cadf982f3baba7`. Both environments use the previously verified
application images tagged `e231fc03`; their application and Docker build inputs match that commit.

## Deployment decision and open evidence

After receiving the two outstanding conditions, the owner explicitly instructed:
“si tu subelo a produccion”. Under the repository's instruction precedence, this direction
authorized this activation despite the unresolved pre-production evidence. It is a release-specific
decision, not a declaration that the public release security gate passed or a change to future gates.

- **UNKNOWN:** revocation of historical Stripe credentials has not been confirmed. No historical
  credential was tested against Stripe, and no credential value belongs in this record.
- **UNKNOWN:** authenticated business acceptance flows in APPTEST were not completed. The available
  synthetic account is a platform administrator whose fictitious email cannot receive the required
  MFA code. MFA was preserved; isolated regression results do not substitute for this acceptance test.

Production Stripe remains disabled as before. No live billing activation, price synchronization,
MFA bypass, or production business-test transactions were performed.

## Verification and preservation

- Previously completed: 1,822 backend tests, 392 frontend tests, TypeScript/Vite build, both deployment
  preflights, 21 MCP tests in the final image, database/object restore rehearsals, and migration/data
  preservation rehearsals. Final application images have no HIGH/CRITICAL scan findings.
- Fresh production database and object backups passed gzip integrity checks; their SHA-256 manifests,
  original Nginx configuration, and protected runtime snapshot remain private on the VPS.
- A dedicated production checkout and dry run preceded activation. Disk capacity was reviewed using
  the runbook's override procedure: 8,398 MiB available before activation, threshold 8,192 MiB.
- The final environment comparison found three missing flags for existing kiosk capabilities.
  Their currently active values were preserved in the protected deployment environment before use:
  `KIOSK_ENGINE_ADAPTER_PETTY_CASH_ENABLED`, `KIOSK_GLOBAL_CENTER_ENABLED`, and
  `KIOSK_MULTI_DASHBOARD_ENABLED`. No application code changed during activation.
- Production applied 13 forward migrations, V252–V264. The original Flyway history was unchanged,
  with zero failed migrations. Catalog prices and subscription counts per catalog version match
  the pre-activation snapshots exactly.
- A read-only audit of 406 declared foreign-key relationships between tables carrying `company_id`
  found zero cross-company mismatches. This is a data consistency check, not a complete authorization audit.
- Production and APPTEST web, backend, MinIO, and MCP containers were running with zero restarts.
  Public HTTPS, storage, CSRF and invalid-login routing passed. Public frontend asset references match
  the deployed image and all eight referenced JS/CSS assets were accessible. MCP rejects anonymous
  access and advertises the expected OAuth metadata.

## Recovery and operational evidence

Previous production application images `2f0bbc5c` and containers remain available under `-rollback`.
Older rollback containers were preserved under distinct timestamped names. The MinIO data mount,
existing credentials, MFA configuration, kiosk capabilities and production datasource were retained.
Application rollback follows `deployment/scripts/rollback-host-network.sh`; it does not reverse
database migrations. Database restoration requires a separate recovery decision.

Private VPS evidence is under `/root/indice-release-20260906/`, including
`production-deployment-decision.json`, `production-activation-result.json`,
`production-post-activation.json`, and
`backups/production-before-activation-20260907T040207Z/`. Do not commit or publish its protected
environments, runtime snapshots, database contents or objects.
