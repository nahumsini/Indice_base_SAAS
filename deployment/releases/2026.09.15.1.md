# v2026.09.15.1 — Actualización del espacio de trabajo de Índice

Producción en `https://app.indiceapp.com` se actualizó el 15 de septiembre de 2026 a las
16:55:30 UTC. El usuario autorizó expresamente el despliegue en producción.

El código publicado corresponde a `e26442a51faccdafb0dc7002275f90f896630c3f`, en la rama
`codex/production-main-20260915`. Incluye el avance de `main`
`a8652bc1b50bf1c71a0466198a97d8f08650ebe0`, integrado con las correcciones publicadas mediante
`5ec906284af6cc966eca51ecfb64724607d61c3f`. Se conserva la línea financiera y de catálogos
que no estaba incluida en main. Los cambios locales sin confirmar del administrador de plataforma
permanecen en la carpeta original y no forman parte de este artefacto.

La actualización incorpora la identidad compartida del espacio de trabajo, preferencias de barras y
pantalla doble, modo aprendiz, notificaciones, moneda y mejoras de presentación en facturación y
kioscos. También incorpora los cambios de main en consulta de tipos de cambio y materialización de
procesos recurrentes. La única corrección adicional es el peso del título de
`IndiceAdminWorkspaceHeader`, ajustado de 600 a 500 según el estándar y su regresión existente.

Las tres imágenes son `amd64` y usan la etiqueta `e26442a51fac`. Los identificadores comprobados
localmente y en el VPS son:

| Imagen | Identificador |
| --- | --- |
| `indice-erp-backend:e26442a51fac` | `sha256:dd74796d3a9e86fac72ec2da51cced1fe11d406733fba17b755ae16efa7fc9b9` |
| `indice-erp-web:e26442a51fac` | `sha256:c91c8850440214d615381dcd3f57dd613a56242783b82f83b382602b5eadf5f8` |
| `indice-erp-mcp:e26442a51fac` | `sha256:41c1051e2c0000c2637baec1dab1a243eafdb839096c313e4dbdaf457be42a8c` |

El backend se construyó con los mismos insumos Java de `5ec90628`; la corrección posterior cambia
exclusivamente una clase CSS del frontend. Se verificó esa igualdad antes de aplicar la etiqueta final.

## Validación y conservación

- Frontend: 690 pruebas aprobadas; TypeScript y build aprobados. La primera ejecución detectó el
  peso tipográfico del encabezado, corregido y verificado con la suite completa.
- Backend: 2,417 pruebas aprobadas localmente y 2,417 en el preflight del VPS, sin fallos, errores
  ni omitidas. Ambas ejecuciones utilizaron bases vacías y aisladas, nunca la base funcional.
- Preflight con la configuración productiva protegida y topología host-network aprobado. Las
  imágenes se construyeron por separado y se transfirieron sin almacenar otro archivo en el VPS.
- Trivy 0.69.3: cero hallazgos HIGH/CRITICAL en las tres imágenes y en las dependencias del
  frontend; cero secretos detectados en los archivos modificados. Se guardaron los SBOM CycloneDX.
  Estos escaneos no equivalen a una certificación integral de seguridad.
- Ensayo de despliegue aprobado antes de reemplazar contenedores.
- Smokes internos y externos aprobados: HTTPS, API, MinIO, CSRF y login con identidad sintética
  inválida, que devolvió el 401 esperado. MCP rechaza acceso anónimo y conserva metadatos OAuth.
- El HTML público coincidió byte por byte con la imagen final. Sus ocho assets de entrada
  respondieron correctamente. El asset principal es `/assets/index-Blbb3R0B.js` y el SHA-256 del
  HTML es `50921eca395f8dc41612a16e5156a529fd13e39710e0deffbbfc6966f6f8be0b`.
- La configuración de negocio, autenticación, datasource y MCP coincidió con la anterior. Se
  conservaron la imagen y el volumen de MinIO. Stripe sigue deshabilitado como estaba.
- Flyway permanece en V276, sin migraciones nuevas ni fallidas. Coincidieron exactamente las
  instantáneas del catálogo de facturación, suscripciones por versión, catálogos públicos y sus
  productos. Las auditorías de alcance y propiedades visuales devolvieron cero incidencias.
- Web, backend, MCP y MinIO quedaron activos, con cero reinicios y cero líneas ERROR en la
  comprobación posterior. APPTEST no fue actualizado.

## Respaldo y reversión

Los respaldos nuevos permanecen protegidos en el VPS:
`/home/corazon/apps/indice-erp-docker/backups/pre-e26442a51fac-20260915T164126Z/`.

- `production.sql.gz`: SHA-256
  `41038910ee8c80b5b622b3e10bc670106f0420c7dea2e98cf4eba31f622d42b2`.
- `minio.tar.gz`: SHA-256
  `67e26fb3514f76fc0c4930ff327a03fe975e0003a5423e24e1a93eee1f91b127`.

Se verificaron gzip y el inventario del archivo de objetos. La restauración de la base en un
contenedor aislado confirmó 314 tablas/vistas, V276 y cero migraciones fallidas. Se restauró y
comprobó una muestra de objeto. El contenedor de comprobación y su volumen se retiraron al terminar.

La versión anterior `8c535a1e9b14` permanece detenida bajo los nombres `indice-erp-*-1-rollback`.
Los contenedores de reversión que existían antes se conservaron con el sufijo
`-rollback-preserved-e26442a51fac`. Ninguna imagen histórica ni volumen productivo fue eliminado.
La reversión de aplicación es compatible con V276:

```bash
CONFIRM_ROLLBACK=true HOST_WEB_PORT=8080 HOST_BACKEND_PORT=8083 \
HOST_MINIO_API_PORT=9000 MCP_ENABLED=true HOST_MCP_PORT=3011 \
bash /root/indice-production-e26442a51fac/source/deployment/scripts/rollback-host-network.sh
```

## Operación y límites

El VPS comenzó con unos 2.5 GiB disponibles. Se retiraron dependencias reconstruibles de liberaciones
antiguas, caché de construcción sin uso y 11 copias de empaquetado idénticas por SHA-256 a JAR
conservados; estas últimas recuperaron 936 MiB. Se excluyeron todos los montajes de contenedores.
Tras respaldar, validar y retirar recursos temporales, quedaron aproximadamente 3,534 MiB libres.
Se revisaron `df` y `docker system df` y se utilizó el ajuste permitido por el runbook:
`DEPLOY_MIN_FREE_MB=3072`, frente al valor predeterminado de 10 GiB. La capacidad del VPS sigue
siendo limitada y requiere seguimiento.

Dos incidencias de las herramientas de validación se resolvieron antes de activar: el preflight
necesitó seleccionar el Java 21 instalado, y la restauración aislada necesitó ampliar su límite de
memoria de 768 a 1,536 MiB. La comprobación externa de Python utilizó el almacén de certificados
del sistema, manteniendo la validación TLS.

No se hicieron transacciones de prueba con cuentas reales, ni UAT manual con MFA o dispositivos
físicos. Cambio de esquema: N/A. Activación comercial de Stripe: N/A.

El checkout productivo dedicado y la evidencia operativa permanecen en
`/root/indice-production-e26442a51fac/`: preflight, ensayos, activación, restauración, auditorías,
configuración protegida anterior, escaneos, SBOM y verificaciones posteriores. Los secretos,
respaldos y datos de clientes no forman parte de este registro ni del repositorio.
