# Gastos: filtros estables y preferencias de columnas — 2026-09-24

Estado: **desplegado en producción** el 24 de septiembre de 2026 a las **19:13:04 UTC**
(15:13 en Toronto), con autorización del usuario para publicar los arreglos de Índice.
Frontend y backend ejecutan `60dc225b462d-r1`; el código corresponde a
`60dc225b462d1d0a981faf04758ef9f648abea74`.

El bloqueo de MySQL público se resolvió después de auditar sus permisos y clientes: únicamente
estaban autorizados localhost, la IP del propio VPS y su nombre DNS; las conexiones activas eran
locales. Se restringió TCP/3306 entrante a loopback, tanto en IPv4 como IPv6, sin reiniciar MySQL.
La comprobación externa confirmó cerrados 3306, 8083, 9000 y 3011; producción y APPTEST siguieron
respondiendo correctamente. Se satisface el
[control de puertos internos](../../docs/indice-public-release-security-gate.md#4-gate-0--scope-and-attack-surface-inventory).

## Candidato y alcance

- Rama: `release/expenses-stability-20260924`.
- Arreglos de aplicación: `9bdee85c15ab0b108d0fad6a7d1dccf277c31900`.
- Candidato con reparación de CI: `60dc225b462d1d0a981faf04758ef9f648abea74`.
- Base: `e7711d5d`, que conserva la presentación `c8bd819c` publicada el 22 de septiembre.
- Se estabilizan los filtros de Gastos, la cancelación de consultas obsoletas y la paginación.
  Columnas conserva visibilidad y orden por empresa/usuario; su entrada de preferencias deja de
  expirar. Aplicar espera confirmación del servidor y permite reintentar un fallo sin perder el
  borrador. La tabla representa el orden elegido.
- Se preservan rutas, permisos, aislamiento, importes, pagos, registros de gastos y la caducidad de
  los otros estados de navegación. No se modifica el catálogo ni las suscripciones.
- El modal de El Corazón y el candidato MCP/OAuth `c41670706db8` no forman parte de esta liberación.
  APPTEST conserva su versión. Migraciones nuevas, cambios de DNS y operaciones Stripe: N/A.

## Validación

- Frontend: TypeScript, build y **828 regresiones** aprobados.
- Chrome: **100 ciclos**, 1.200 gastos sintéticos y 200 filas; cero errores. Incluye filtros,
  orden, paginación, preferencias, arrastre, fallo/reintento, cambio de pestaña, sesión y móvil.
- Backend: preflight con el entorno productivo protegido y MySQL temporal aislado:
  **2.468 pruebas**, cero fallos/errores/omisiones; MCP: **27 pruebas**.
- El preflight completo comprobó `9bdee85c`. La diferencia de `60dc225b` es exclusivamente
  `.github/workflows/ci.yml` y `deployment/docker/ci-minio/Dockerfile`; se comprobó igualdad del
  árbol de aplicación, recursos, dependencias, scripts y configuración de despliegue. CI vuelve
  a ejecutar la suite completa sobre el commit final.
- [CI del candidato](https://github.com/nahumsini/Indice_base_SAAS/actions/runs/36041745154): **aprobado** (commit exacto `60dc225b`).
- El [primer CI](https://github.com/nahumsini/Indice_base_SAAS/actions/runs/36040607139) aprobó las
  suites pero falló antes del smoke porque Quay rechazó la descarga de MinIO. La reparación
  construye únicamente la dependencia de CI desde el [commit oficial de la misma versión](https://github.com/minio/minio/commit/07c3a429bfed433e49018cb0f78a52145d4bedeb),
  sin cambiar MinIO productivo ni adoptar una imagen de terceros.
- Escaneo final Trivy: cero hallazgos HIGH/CRITICAL en backend, web y MCP conservado;
  SBOM CycloneDX guardados. Cero secretos en código e historial incorporado (Gitleaks 8.24.2).
- El escaneo inicial encontró `CVE-2026-93990` en `libexpat 2.8.4-r0` de los runtimes heredados.
  Se actualizó exclusivamente esa biblioteca en los candidatos y se repitió el escaneo. Las
  primeras imágenes sin sufijo no son artefactos autorizados para activar; se usan las `-r1`.
- Java y Nginx con el montaje productivo: aprobados. El HTML de la imagen coincide con el
  build del checkout. El ensayo canónico `DEPLOY_DRY_RUN=true` y el ensayo del procedimiento de
  activación/rollback aprobaron con el umbral normal de 10 GiB. El ensayo precedió a la activación satisfactoria.
- La comprobación inicial de Nginx sin el montaje esperado falló al resolver `minio`; la
  comprobación final usa exactamente la configuración host-network que se conservará.
- A las 18:40:11 UTC, health público respondió 200. Producción y APPTEST conservaron identificadores,
  horas de arranque y reinicios; configuración y HTML productivos conservaron sus huellas.
- Aviso conocido de tamaño de chunks de Vite; no impide la compilación.

## Artefactos, respaldo y reversión

Imágenes construidas con los Dockerfiles `prebuilt` del repositorio, el JAR/dist verificados y
los runtimes anteriores identificados por digest, actualizando `libexpat` y conservando ejecución
sin privilegios:

| Imagen | ID |
| --- | --- |
| `indice-erp-backend:60dc225b462d-r1` | `sha256:d2086d0de71ef34e514a10ea49bdc95257732cad515134479b411d9871a52dd1` |
| `indice-erp-web:60dc225b462d-r1` | `sha256:3f82c93aec688778b21bd1f2dee798d3fbca075522a363b66cd728ae8559bf55` |

Evidencia y scripts privados: `/root/indice-expenses-9bdee85c15ab/`. El nombre del directorio
conserva el SHA inicial; su checkout y las imágenes corresponden al candidato final indicado.
No se incorporan credenciales, filas de clientes ni respaldos al repositorio.

El respaldo `production-backup/` se restauró a las **18:34:08 UTC** en MySQL sin red:
314 tablas/vistas, Flyway V276 y cero migraciones fallidas. Se restauraron y verificaron por hash
los **3.759 archivos** del almacenamiento. Contenedores, volúmenes y credenciales temporales del
ensayo se retiraron. Las huellas del catálogo y suscripciones se conservan para comparación.

La activación reemplazó únicamente backend y web de forma secuencial, conservó exactamente
configuración y montajes y retuvo los contenedores anteriores para reversión. Es necesario conservar
el contenedor web actual, porque contiene la presentación publicada después de su imagen base.
El script exige CI, escaneos, restauración, ensayo previo y control de red antes de activar.
MCP, MinIO, bases de datos y APPTEST permanecen operando en sus contenedores existentes.

Se liberaron solamente caché de compilación y dependencias `node_modules` regenerables de checkouts
sin procesos activos. Código, lockfiles, imágenes, datos y respaldos se conservaron. El margen de
10 GiB aprobó el ensayo y la activación. Después quedaron 10.559 MiB disponibles; el margen es
reducido y deberá revisarse en la siguiente liberación. No se redujo el umbral de despliegue.

## Resultado y operación

- Smokes de web, backend, MinIO, CSRF y login sintético inválido: aprobados.
- HTML público idéntico al artefacto desplegado y sus ocho assets iniciales disponibles.
  SHA-256: `974ec60ad3c85a9c73fd3d8f77414e132b9bed28de52a4baaf4e46ed55a8c0f6`.
- Chrome en una sesión nueva entró a Gastos con «Pagado / Mes pasado» y redirigió correctamente a
  login; cero errores JavaScript, recursos fallidos o pantalla de error. No se usó una cuenta real.
- Comprobación posterior: backend/web activos, cero reinicios y cero líneas ERROR/FATAL.
  La revisión de identidad/configuración de los contenedores retenidos para rollback aprobó.
- Gastos y preferencias de columnas siguen rechazando acceso anónimo con HTTP 401.
- Cookies conservan `Secure` y `HttpOnly`; metadatos OAuth y protección MCP: aprobados.
- Huellas de catálogo de facturación y suscripciones sin cambios respecto a la lectura inmediata
  anterior a la activación. Flyway permanece en V276, sin migraciones fallidas.
- MCP, MinIO, bases de datos y APPTEST conservaron sus contenedores y horas de arranque.
- No se introdujeron credenciales, datos financieros ni operaciones con cuentas reales de usuario.
  El ensayo de filtros/columnas usa datos sintéticos; la sesión real del usuario no se utilizó.

Las reglas se respaldaron en `/root/indice-expenses-9bdee85c15ab/network-3306-backup-local-probes/`.
Se conservaron el resto del firewall y los accesos locales, incluyendo conexiones del propio VPS
hacia su IP pública. `/etc/sysconfig/iptables` e `ip6tables` incluyen la restricción validada, y
`indice-mysql-local-only.service` la comprueba al arrancar sin vaciar reglas ajenas.
El primer intento se revirtió automáticamente por una comprobación que esperaba que `root`
pudiera autenticarse por la IP pública; esa cuenta solamente autoriza localhost. El ensayo final
comprueba SQL en localhost, conectividad local a la IP del VPS y la salud de ambas aplicaciones.
No se cambió ningún permiso de base de datos.

Los contenedores anteriores permanecen detenidos como:

- `indice-erp-backend-1-rollback-60dc225b462d-r1`
- `indice-erp-web-1-rollback-60dc225b462d-r1`

El web retenido conserva también la presentación publicada el 22 de septiembre. Para comprobar la
reversión sin activarla: `python3 /root/indice-expenses-9bdee85c15ab/rollback.py --check`.
La ejecución explícita usa `--execute`, restaura esos contenedores y mantiene datos, almacenamiento
y protección de MySQL. Es compatible con V276; no requiere restaurar la base de datos.
