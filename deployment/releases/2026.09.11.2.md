# v2026.09.11.2 — Petty Cash statement report remaster

Deployed frontend commit: `c77575388c56d3e806dc9d275ed3c931acb39b7d` from `release/2026-09-11-finance-corrections`.
Previous public frontend asset: `/assets/index-CNP2OUgJ.js`.
Deployed public frontend asset: `/assets/index-BTb-iT6x.js`.

This frontend-only release remasters the Petty Cash statement preview, PDF download and print output with the same restrained financial-document treatment used by Expenses. The three outputs now share one document definition, retain the complete statement, movement, receipt and reconciliation detail, and use a single green accent with neutral metadata and tables. It also includes the aligned Petty Cash title-bar action hierarchy from `fb6a5773`.

Local validation passed TypeScript, the production frontend build, 47 Petty Cash UI regressions and 6 print regressions. The generated A4 fixture was rendered to two pages and inspected for wrapping, clipping, pagination and content consistency. GitHub Actions run `34639453471` passed Flyway validation, the backend suite, TypeScript, frontend build, frontend regressions, Compose validation and backend image construction. Its backend runtime smoke could not start because Docker Hub rejected the external `minio/minio:latest` pull on two attempts; the application image itself was not started by that step.

Production was healthy before activation. The documented frontend-only publisher copied the verified `react/dist` bundle into `indice-erp-web-1` without changing Nginx, backend, database, migrations or storage. The package SHA-256 was `4a00893361fd90812aa5d4521dd1dafd5d78bd433360410dc8a346fe6802dcd9`. Public checks returned HTTP 200 for `/`, `/petty-cash/cash`, `/petty-cash/control` and `/expenses/expenses`; the official smoke passed web, backend health through the proxy and MinIO storage. Web and backend remained running with zero restarts.

Rollback copy: `/root/indice-frontend-c7757538/rollback-dist` on the VPS. Restoring that directory to `/usr/share/nginx/html` in `indice-erp-web-1` returns the public index to `/assets/index-CNP2OUgJ.js`. The immutable backend and its runtime configuration remain those of `v2026.09.11.1`.
