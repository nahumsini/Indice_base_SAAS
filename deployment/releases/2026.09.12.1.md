# v2026.09.12.1 — Experiencia configurable del catálogo público

Aplicación desplegada desde el commit `a4ddd40fb0b79a6568e620d1960d2823d0916c22` de
`release/2026-09-11-finance-corrections`. Las imágenes activas en APPTEST y producción son
`indice-erp-web:a4ddd40fb0b7` e `indice-erp-backend:a4ddd40fb0b7`; MCP conserva la imagen
`indice-erp-mcp:e231fc03`.

El editor del catálogo público ahora usa un flujo de cuatro pasos para identidad, apariencia,
funciones, productos y revisión. Permite elegir perfil de negocio, color de marca, portada,
distribución, acabado y proporción de imágenes, muestra una vista previa viva de la tarjeta y
conserva la navegación al editar. El backend persiste estas opciones mediante la migración aditiva
`V276__public_catalog_visual_experience.sql`, con valores predeterminados compatibles para los
catálogos existentes. El guardado de catálogos grandes usa lotes y admite hasta 2,000 productos,
evitando el fallo de validación que mostraba en pantalla la lista completa de identificadores.

Se conserva la renovación de enlaces firmados incorporada en `v2026.09.11.4`: el catálogo actualiza
el bootstrap cada diez minutos, al recuperar visibilidad o conexión, y reintenta una imagen de
galería una vez con un enlace nuevo. La evidencia de esa liberación verificó las 32 imágenes de
`Villa Quetzal 24-02` contra el proxy del mismo origen. Esta liberación mantiene ese comportamiento
y no cambia la privacidad de los objetos, el carrito ni las solicitudes de compra. Después de la
activación, una comprobación interna de sólo lectura volvió a encontrar 32 referencias de esa villa,
32 objetos presentes en MinIO y cero objetos faltantes.

La ejecución CI `34675735763` pasó las 2,357 pruebas de backend, migraciones, TypeScript, build de
producción, regresiones de frontend, validación de Compose, construcción de imágenes y smoke de
arranque del backend. El smoke de MinIO quedó fijado a la imagen oficial versionada de Quay para
evitar la dependencia de una etiqueta inexistente en Docker Hub.

Antes de activar producción se ejecutaron los preflights con los archivos de entorno reales, un
ensayo sobre una restauración aislada aplicó V276 y arrancó también la imagen de reversión, y el dry
run validó imágenes, puertos y espacio. APPTEST se actualizó primero y pasó los smokes de web,
backend, MinIO, CSRF, login sintético y MCP/OAuth. Producción pasó las mismas comprobaciones con
cero reinicios de web y backend.

Las instantáneas ordenadas antes y después coinciden byte por byte para catálogo comercial de
facturación, asociaciones de suscripciones, los 7 catálogos públicos y sus 110 asociaciones de
productos. Flyway quedó en 276 sin migraciones fallidas y las seis nuevas propiedades visuales no
tienen valores nulos ni fuera de catálogo. Las auditorías de alcance de catálogo, kioscos y cajas
permanecen sin registros no resolubles.

El respaldo transaccional final está en
`/home/corazon/apps/indice-erp-docker/backups/pre-a4ddd40fb0b7-openai-20260912012206/production.sql.gz`,
comprimido, no vacío y acompañado por su SHA-256. Un respaldo anterior del mismo corte se restauró
en MySQL 8 aislado y validó 314 tablas y Flyway 275 antes del ensayo de migración.

La configuración persistente de producción corrigió las tres URLs públicas de Stripe que contenían
formato Markdown y dejó `APP_BILLING_STRIPE_ENABLED=false` y
`APP_BILLING_STRIPE_PROCESSOR_ENABLED=false`, iguales al comportamiento del contenedor anterior.
La copia previa se conserva en
`/root/indice-public-catalog-d0ee08e9/production.env.before-stripe-url-fix` con checksum. No se
publicaron precios, no se ejecutaron sincronizaciones de Stripe y no cambiaron datos de cobro.

La reversión conserva web, MinIO y MCP anteriores. El backend anterior está preparado como
`indice-erp-backend:253dd88d8ec2-rollback-v276`: contiene exactamente la aplicación previa más la
migración V276 necesaria para arrancar sobre el esquema actual. El ensayo de reversión pasó contra
la copia restaurada; la migración es aditiva y la reversión de aplicación conserva sus columnas y
datos.
