# Vite proxy target for local development. Browser requests stay on /api unless
# VITE_API_BASE_URL is explicitly set.
VITE_BACKEND_URL=http://127.0.0.1:8082

# Optional public API origin for deployments without a same-origin proxy.
VITE_API_BASE_URL=
